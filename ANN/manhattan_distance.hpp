#ifndef MANHATTAN_DISTANCE_HPP
#define MANHATTAN_DISTANCE_HPP

float manhattan_distance(float*, float*, int);

int manhattan_distance(unsigned char*, unsigned char*, int);

int manhattan_distance(unsigned short*, unsigned short*, int);

float manhattan_distance(float*, unsigned char*, int);

#endif
