//brute_nn.hpp

#include <vector>
#include "point.hpp"
#include "small_point.hpp"

Point*  brute_nn(unsigned char*, int,int ,std::vector <Point*>* );

small_Point*  small_brute_nn(unsigned short*, int,int ,std::vector <small_Point*>* );